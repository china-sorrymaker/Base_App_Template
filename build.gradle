// 根目录 build.gradle
buildscript {
    // 仅保留 buildscript 内部的仓库，用于下载插件
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
