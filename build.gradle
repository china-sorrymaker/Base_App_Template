buildscript {
    repositories { google(); mavenCentral() }
    dependencies {
        // 这是安卓开发的“发动机”
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

allprojects {
    repositories { google(); mavenCentral() }
}
